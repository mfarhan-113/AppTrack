services:
  - type: web
    name: apptrack-backend
    env: python
    plan: free
    region: frankfurt
    rootDir: .
    buildCommand: pip install -r apptrack-backend/requirements.txt
    startCommand: python apptrack-backend/manage.py migrate --noinput && gunicorn apptrack-backend/config.wsgi:application
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: DATABASE_URL
        fromDatabase:
          name: apptrack-postgres
          property: connectionString
      - key: ALLOWED_HOSTS
        value: your-backend.onrender.com,your-frontend.vercel.app

databases:
  - name: apptrack-postgres
    plan: free